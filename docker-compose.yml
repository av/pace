services:
  pace:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      # Uncomment to use custom config (create config.yaml from config.example.yaml first):
      # - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped
